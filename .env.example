# =============================================================================
# PORTFOLIONE - MINIAPP ENVIRONMENT VARIABLES
# =============================================================================

# -----------------------------------------------------------------------------
# ALCHEMY ACCOUNT KIT (Required for wallet functionality)
# Get these from: https://dashboard.alchemy.com/
# -----------------------------------------------------------------------------
NEXT_PUBLIC_ALCHEMY_API_KEY=YOUR_ALCHEMY_API_KEY
# Used in: config.ts - Creates Alchemy transport for Account Kit
# Purpose: Enables smart wallet creation and transaction sponsorship

NEXT_PUBLIC_ALCHEMY_POLICY_ID=YOUR_ALCHEMY_POLICY_ID  
# Used in: config.ts - Gas sponsorship policy for Account Kit
# Purpose: Sponsors gas fees for user transactions

NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=YOUR_WALLETCONNECT_PROJECT_ID
# Used in: config.ts - WalletConnect integration (even if commented out)
# Purpose: Required by Account Kit configuration

# -----------------------------------------------------------------------------
# MINIKIT CORE (Required for MiniApp functionality)
# Get CDP API key from: https://portal.cdp.coinbase.com/
# -----------------------------------------------------------------------------
NEXT_PUBLIC_ONCHAINKIT_PROJECT_NAME=Portfolione
# Used in: app/layout.tsx (generateMetadata), app/.well-known/farcaster.json/route.ts
# Purpose: App name displayed in Base App and Farcaster

NEXT_PUBLIC_URL=https://your-app.vercel.app
# Used in: app/layout.tsx (generateMetadata), app/.well-known/farcaster.json/route.ts
# Purpose: Base URL for manifest and frame metadata

NEXT_PUBLIC_ONCHAINKIT_API_KEY=your_cdp_client_api_key
# Used in: providers/MiniKitProvider.tsx
# Purpose: Coinbase Developer Platform API key for MiniKit functionality

# -----------------------------------------------------------------------------
# FARCASTER MANIFEST (Generated by `npx create-onchain --manifest`)
# Run this command after deploying to generate these values
# -----------------------------------------------------------------------------
FARCASTER_HEADER=base64_encoded_header
# Used in: app/.well-known/farcaster.json/route.ts
# Purpose: Proves ownership of your Farcaster account for app verification

FARCASTER_PAYLOAD=base64_encoded_payload
# Used in: app/.well-known/farcaster.json/route.ts  
# Purpose: Contains account association data for Farcaster verification

FARCASTER_SIGNATURE=hex_signature
# Used in: app/.well-known/farcaster.json/route.ts
# Purpose: Cryptographic signature proving account ownership

# -----------------------------------------------------------------------------
# MINIAPP METADATA (Optional but recommended for better UX)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_SUBTITLE=Smart Portfolio Rebalancing
# Used in: app/layout.tsx (generateMetadata), app/.well-known/farcaster.json/route.ts
# Purpose: Short description shown in app listings

NEXT_PUBLIC_APP_DESCRIPTION=One-click portfolio rebalancing on Base mainnet with Alchemy Account Kit
# Used in: app/layout.tsx (generateMetadata), app/.well-known/farcaster.json/route.ts
# Purpose: Detailed description for app discovery and SEO

NEXT_PUBLIC_APP_PRIMARY_CATEGORY=defi
# Used in: app/.well-known/farcaster.json/route.ts
# Purpose: Category for app discovery in Base App catalog

NEXT_PUBLIC_APP_TAGLINE=Rebalance instantly
# Used in: app/layout.tsx (generateMetadata), app/.well-known/farcaster.json/route.ts
# Purpose: Short catchy phrase for marketing

NEXT_PUBLIC_SPLASH_BACKGROUND_COLOR=#6366f1
# Used in: app/layout.tsx (generateMetadata), app/.well-known/farcaster.json/route.ts
# Purpose: Background color shown during app loading

# -----------------------------------------------------------------------------
# SOCIAL SHARING & SEO (Optional)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_ICON=https://your-app.vercel.app/icon.png
# Used in: app/.well-known/farcaster.json/route.ts
# Purpose: App icon shown in Base App and social shares

NEXT_PUBLIC_APP_SPLASH_IMAGE=https://your-app.vercel.app/splash.png
# Used in: app/layout.tsx (generateMetadata), app/.well-known/farcaster.json/route.ts
# Purpose: Image shown while app is loading

NEXT_PUBLIC_APP_HERO_IMAGE=https://your-app.vercel.app/og.png
# Used in: app/layout.tsx (generateMetadata), app/.well-known/farcaster.json/route.ts
# Purpose: Large image for social media embeds and app previews

NEXT_PUBLIC_APP_OG_TITLE=Portfolione - Smart Portfolio Rebalancing
# Used in: app/layout.tsx (generateMetadata), app/.well-known/farcaster.json/route.ts
# Purpose: Title for Open Graph social media previews

NEXT_PUBLIC_APP_OG_DESCRIPTION=One-click portfolio rebalancing on Base mainnet
# Used in: app/layout.tsx (generateMetadata), app/.well-known/farcaster.json/route.ts
# Purpose: Description for Open Graph social media previews

NEXT_PUBLIC_APP_OG_IMAGE=https://your-app.vercel.app/og.png
# Used in: app/layout.tsx (generateMetadata), app/.well-known/farcaster.json/route.ts
# Purpose: Image for Open Graph social media previews

# =============================================================================
# DEPLOYMENT NOTES:
# 1. Deploy your app to get a stable HTTPS URL
# 2. Update NEXT_PUBLIC_URL with your deployed URL
# 3. Run `npx create-onchain --manifest` to generate Farcaster credentials
# 4. Upload assets (icon.png, splash.png, og.png) to your public directory
# 5. Test manifest at: https://your-app.vercel.app/.well-known/farcaster.json
# =============================================================================